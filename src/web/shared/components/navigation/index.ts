// Import the Breadcrumbs component for re-export
import Breadcrumbs from './Breadcrumbs';
// Import the NavBar component for re-export
import NavBar, { NavBarProps, NavItem } from './NavBar';
// Import the Pagination component for re-export
import Pagination, { PaginationProps } from './Pagination';
// Import the Tabs component for re-export
import Tabs, { TabsProps, TabItem } from './Tabs';
// Import the BottomNavigation component for re-export
import BottomNavigation, { BottomNavigationProps, BottomNavigationItem } from './BottomNavigation';

// Export the Breadcrumbs component for navigation trails
export { Breadcrumbs };

// Export the NavBar component for top navigation
export { NavBar };

// Export the NavBarProps interface for type checking
export type { NavBarProps };

// Export the NavItem interface for navigation item configuration
export type { NavItem };

// Export the Pagination component for paginated content navigation
export { Pagination };

// Export the PaginationProps interface for type checking
export type { PaginationProps };

// Export the Tabs component for tabbed content navigation
export { Tabs };

// Export the TabsProps interface for type checking
export type { TabsProps };

// Export the TabItem interface for tab configuration
export type { TabItem };

// Export the BottomNavigation component for mobile bottom navigation
export { BottomNavigation };

// Export the BottomNavigationProps interface for type checking
export type { BottomNavigationProps };

// Export the BottomNavigationItem interface for bottom navigation item configuration
export type { BottomNavigationItem };